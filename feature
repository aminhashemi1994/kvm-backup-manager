feature request : 

1. in scheduler when a backup fails we need to retry for 3 times and if the backup still fails then we do nothing but the scheduler must stay intact

2. add a setting for each scheduler so user can set a retry for the backups how many times to retry and if they want a delay in the backup retry. 

the default is 3 and retry is 5 minutes after each time

3. all the confirmation alerts in the front must be removed and replaces with a popup windows confirmation with the exact required inputs. 